DROP TABLE IF EXISTS TASK;

CREATE TABLE TASK
(
   id BIGINT AUTO_INCREMENT PRIMARY KEY NOT NULL
  ,title VARCHAR2(200) NOT NULL
  ,regDate DATE NOT NULL
  ,updDate DATE
  ,finYn VARCHAR2(1) DEFAULT 'N'
  ,parentId VARCHAR2(1)
);
